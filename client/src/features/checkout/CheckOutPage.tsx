import { Typography } from "@mui/material";

export default function CheckOutPage() {
  return (
    <Typography variant="h3">
      Only logged users should able to see this!
    </Typography>
  );
}
